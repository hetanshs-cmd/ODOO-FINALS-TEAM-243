/**
 * DealFlow360 API & Service Layer Interface
 * Unified typed service layer re-exporting from index.ts.
 * Communicates directly with the authoritative store and domain engines.
 */

export * from './index';
export { api as default } from './index';
